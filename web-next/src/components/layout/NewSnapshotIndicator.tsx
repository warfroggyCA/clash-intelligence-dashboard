"use client";

import React from 'react';

// NewSnapshotIndicator retired - pages now fetch their own data
// No longer needed with SSOT architecture where each page fetches directly
export default function NewSnapshotIndicator() {
  return null;
}
