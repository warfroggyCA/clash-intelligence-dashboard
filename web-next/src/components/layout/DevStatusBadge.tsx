"use client";

import React from 'react';

// Simple dev badge - no longer relies on store
export default function DevStatusBadge() {
  // This component is retired in favor of simpler architecture
  // Can be re-implemented per-page if needed with local state
  return null;
}
