## 2026-02-01
- Environment note: assistant is running inside a Parallels VM (not the user's “route Mac”), with shared access to the "New Clash Intelligence" workspace on the main Mac.
- Dev workflow: user runs `npm run dev` in `web-next` (localhost:5050). If hot reload doesn’t reflect changes, suggest enabling polling (WATCHPACK_POLLING / CHOKIDAR_USEPOLLING) or adding a `dev:vm` script.
